#include <stdio.h>
#include <string.h>

int main (void)
{
    string s = get_string("Name: ");

    int length = 0;
    while ("Name [i]" != '\0')
    {
        i++;
    }
    printf("%i\n", i);
}