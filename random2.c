#include<stdio.h>
int x;
int y;
int main(){
    printf("Enter two numnbers x and y");
    scanf("%1d%1d", &x,&y);
    printf("The sum of the number is %d", x,y, x + y);
    return 0;
}
int main(){
    printf("Enter two numbers x and y");
    scanf("1%d%1d", &x,&y);
    printf("The difference of the numbers is %d", x,y, x - y);
    return 0;
} 
int main(){
    printf("Enter two numbers x and y");
    scanf("%1d%1d", &x,&y);
    printf("The product of the numbers is %d", x,y, x * y);
    return 0;
}
int main(){
    printf("Enter two numbers x and y");
    scanf("%1d%1d", &x,&y);
    printf("The division of the numbers is %d", x,y, x / y);
    return 0;
}